---
# Waydroid install

- name: Add repo key
  apt_key:
    url: https://repo.waydro.id/waydroid.gpg
    state: present


- name: Add repo
  apt_repository:
    repo: deb [arch=amd64] https://repo.waydro.id/ jammy main
    state: present
    filename: waydroid

- name: Add Waydroid
  apt:
    name: waydroid
